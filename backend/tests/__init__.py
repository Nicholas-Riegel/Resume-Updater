# backend/tests/__init__.py
#
# This file marks tests/ as a Python package.
# It doesn't need any content — its presence is enough to make Python
# treat the folder as a package, which ensures imports resolve correctly
# when pytest collects test files.
